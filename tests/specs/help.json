[
  {
    "name": "/help explains moderation actions",
    "steps": [
      {
        "send": { "text": "/help" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Use /start in a group to open the moderation menu.\n\nReply to a member with /warn, /mute 10m, or /kick.\nAdmins can use /setwelcome, /setrules, /setautoactions, /log, and /overview."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "the Help menu button shows the same help text",
    "steps": [
      {
        "send": { "callback": "menu:help" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
